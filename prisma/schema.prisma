// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Transaction {
  BUY
  SELL
  INIT
}

model Stock {
  id                    Int         @id @default(autoincrement())
  tradingsymbol         String      @unique
  quantity              Int         @default(0)
  average_price         Float       @default(0)
  last_action_price     Float       @default(0)
  last_transaction      Transaction @default(INIT)
  last_action_timestamp DateTime    @updatedAt
  pnl                   Float       @default(0)
  // foreign key
  piggyBankId           Int         @default(1)
  // prisma internal relation
  PiggyBank             PiggyBank   @relation(fields: [piggyBankId], references: [id])
}

model PiggyBank {
  id      Int     @id @default(1)
  balance Float
  stocks  Stock[]
}
